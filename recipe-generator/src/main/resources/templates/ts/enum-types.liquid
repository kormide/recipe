export class {{enum.name}} {
    {% for value in enum.values %}
    static readonly {{value}}: {{enum.name}};
    {% endfor %}
    private value;
    private constructor();
    getValue(): string;
    toJSON(): any;
    fromJSON(json: any): {{enum.name}};
}
