ingredients:
  - name: "EmptyIngredient"

  - name: "IngredientWithRequiredProperty"
    required:
      - name: "required"
        type: "string"
    initializers:
      - params:
        - "required"