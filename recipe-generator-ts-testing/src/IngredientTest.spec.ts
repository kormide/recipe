import { EmptyIngredient } from "../target/ingredients/EmptyIngredient";

describe("generation", () => {
    it("should generate an empty ingredient", () => {
        new EmptyIngredient();
    });
});